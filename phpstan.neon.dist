includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 5
    paths:
        - src
        - config

    # config/cdn.php is published into the host application's config directory,
    # where env() calls are the documented way to configure the package.
    noEnvCallsOutsideOfConfig: false
